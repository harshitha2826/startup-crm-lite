# Frontend is served by Vite on port 5173.
# All /api/* requests are proxied to localhost:5000 by vite.config.js.
# Do NOT set VITE_API_URL here in local dev — let the proxy handle it.
# VITE_API_URL=https://your-backend.onrender.com  <-- uncomment only for production builds
